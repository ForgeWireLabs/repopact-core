{
  "$schema": "../../repopact/schemas/evidence-run.schema.json",
  "id": "20260923-072-personal-repository-identity",
  "timestamp": "2026-09-23T22:03:50Z",
  "work_item": "072",
  "result": "partial",
  "provenance": "concrete",
  "commands": [
    {
      "command": "git remote set-url origin https://github.com/JeremyShows/repopact.git; git remote get-url --all origin; git remote get-url --push --all origin",
      "exit_code": 0,
      "summary": "Both fetch and push URLs directly name JeremyShows/repopact."
    },
    {
      "command": "rg -n -i --hidden -g '!\\.git/**' -g '!evidence/**' -g '!work/completed/**' -g '!research/case-studies/**' 'ForgeWireLabs/repopact' .",
      "exit_code": 0,
      "summary": "Remaining matches are the separate RepoPact Proving Ground repository, historical decision/research records, and this work item's explanation of the former location; current RepoPact self-links use JeremyShows/repopact. The Proving Ground owner was verified in a subsequent follow-up audit."
    },
    {
      "command": "cargo build --release -p repopact-engine",
      "exit_code": 0,
      "summary": "With VERSION 3.1.3 and RELEASE_LABEL 3.1.3-dev.1, the checkout engine rebuilt and reports engine_version 3.1.3.dev1, matching the package development identity. The system-wide Scripts executable remains stale at 3.1.0.dev1, so validation used REPOPACT_ENGINE pointed at the checkout build."
    },
    {
      "command": "REPOPACT_ENGINE=rust/target/release/repopact-engine.exe; python -m unittest discover -s tests -p test_graph_cli.py -v; python -m unittest discover -s tests -p test_engine_client.py -v",
      "exit_code": 0,
      "summary": "All 21 graph CLI tests and all 6 engine-client tests passed with the matching checkout engine."
    },
    {
      "command": "python -m unittest discover -s tests -v",
      "exit_code": 1,
      "summary": "373 tests ran: 18 failures, 22 errors, and 3 skips. This run used the stale system engine and therefore included engine-version errors; it also exposed repository fixture/research freshness drift. Focused graph and engine-client suites passed after rebuilding the matching engine."
    },
    {
      "command": "REPOPACT_ENGINE=rust/target/release/repopact-engine.exe; repopact dashboard --root .; repopact validate",
      "exit_code": 1,
      "summary": "Dashboard regeneration passed. Repository validation reported the existing research freshness expiry (contract expired 2026-09-18) and, before this manifest was added, the expected unresolved evidence reference for this run. The research claim was not refreshed because it requires substantive re-verification outside this identity change."
    },
    {
      "command": "git ls-remote --symref origin HEAD refs/heads/main",
      "exit_code": 128,
      "summary": "Direct access to the corrected personal URL still returns HTTP 403: GitHub says the repository/account Git service is disabled. GitHub API metadata identifies JeremyShows/repopact as the current repository, but no remote writes or branch cleanup were attempted."
    }
  ],
  "artifacts": [
    "README.md",
    "CONTRIBUTING.md",
    "ROADMAP.md",
    "CITATION.cff",
    "pyproject.toml",
    "RELEASE_LABEL",
    ".github/ISSUE_TEMPLATE/config.yml",
    "governance/adopters.json",
    "rust/crates/repopact-validation/src/adopters.rs",
    "rust/crates/repopact-provider-github/src/headers.rs",
    "tests/test_fleet_verify.py",
    "work/completed/072-personal-repository-identity/work-item.json"
  ],
  "environment": {
    "os": "Windows",
    "python": "3.12",
    "repository": "C:\\local-path-redacted",
    "main_commit": "686c986de9f8fa1e73679e5d34d51cfc99af5363",
    "package_identity": "3.1.3.dev1",
    "checkout_engine": "rust/target/release/repopact-engine.exe",
    "remote_access": "blocked by GitHub HTTP 403"
  }
}
