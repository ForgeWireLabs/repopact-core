# Next bounded WI074 slice: Core packaging, CLI compatibility and conformance

**Planning only.** This directive does not authorize implementation. Begin only
after a separate operator authorization, from Core `main` at the published
RPS-001/RPS-002/RPS-017 closeout revision. Verify the remote baseline and use a
fresh isolated worktree. RepoPact **3.1.3 remains the current stable version**;
the extracted development checkout's `3.1.3.dev1` identity is not a stable
release.

## Scope

Work only on these existing WI074 criteria:

- **RPS-004 — Core build:** From a clean standalone Core checkout with no
  Workbench tree, build and inspect the Python wheel, source distribution and
  native Rust engine. Record artifact hashes, contents, version metadata,
  engine identity and a clean installation/CLI smoke test.
- **RPS-005 — CLI compatibility:** Enumerate the documented stable 3.1.3
  headless commands and options from the frozen 3.1.3 oracle. Prove the
  extracted CLI preserves them. Any incompatible change requires explicit
  operator authorization and versioning; do not silently redefine stable
  behavior.
- **RPS-006 — install identity:** Verify the `repopact` package and command
  names, matching `repopact-engine` identity and fail-closed protocol/version
  handshake across build, install and invocation.
- **RPS-014 — conformance:** Run the frozen 3.1.3 fixture oracle and coverage
  checks against standalone Core, including diagnostic identity and Python
  regression comparison. Preserve actual mismatches; do not weaken fixtures or
  acceptance rules.

## Validation and boundaries

Use project-local tooling and record exact commands, environment, revision,
counts, artifact hashes and limitations. Run focused regression tests,
canonical validation, schema/reference checks and frozen-surface verification.
Use clean build and install directories outside all source checkouts. Reconcile
any generated dashboard or evidence through the documented process.

Do not publish to PyPI, create or modify repositories, enable Actions, invoke
paid services, change Workbench's Core pin, migrate ForgeWire or Proving Ground,
or claim platform, Android-feature, installer, rollback or downstream
compatibility acceptance in this slice. Leave RPS-001, RPS-002 and RPS-017
evidence immutable.

The slice is complete only when each of RPS-004, RPS-005, RPS-006 and RPS-014
has criterion-specific evidence and the canonical work item reflects only
proved acceptance states.
