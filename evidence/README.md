# Evidence Ledger

Evidence manifests are immutable records of validation runs. Use a stable ID such
as `YYYYMMDD-HHMMSS-short-name.json`. A rerun creates a new manifest rather than
editing an old result.

Work-item acceptance criteria link evidence by run ID. Artifact paths are relative
to the repository root or external immutable URLs.

