# 00 — Evidence Freeze (Phase 1)

Captured 2026-08-21. Nothing in the subject repositories was cleaned, staged,
committed, or otherwise altered to produce this snapshot. All commands were
read-only (`git rev-parse`, `git status`, `git log`, `ls`/`find`).

## ForgeWire

- Path: `C:\\local-path-redacted
- Branch: `main`
- HEAD: `229156e1355f42d0762ddd7ca5784ed95dccf683`
- Working tree: clean except one untracked, gitignored directory
  (`artifacts/llm-parity/`, generated by the LLM parity harness step of
  `scripts/ci.py full`; excluded via `.gitignore:204`).

### Relevant commit chain (all on `main`, in order)

| SHA | Subject |
| --- | --- |
| `2b114263` | Close WI230: ADR, docs, closeout evidence (FMR-014/FMR-015) |
| `e02aec1e` | Reconcile RepoPact governance to zero errors (WI236 part 1/4) |
| `0d3d7608` | Fix pre-existing gate violations surfaced while building the canonical runner (WI236 part 2/4) |
| `c4b1c363` | Add canonical scripts/ci.py fast/full/closeout runner (WI236 part 3/4) |
| `5f05b3e2` | Wire hosted CI to invoke the canonical validation runner (WI236 part 4/4) |
| `cb91fea6` | Propose WI236 causal state runtime substrate *(landed on origin/main independently, concurrently, under the same id)* |
| `fa78d397` | Add RepoPact ledger for WI236 *(companion commit to `cb91fea6`)* |
| `658333d9` | Renumber this work item from 236 to 237 (origin/main ID collision) |
| `b3668a32` | Merge origin/main (WI236 causal-state-runtime-substrate proposal) |
| `4e9edc08` | Regenerate dashboard after merge |
| `229156e1` | Record WI237 closeout evidence |

Note on ordering: `cb91fea6`/`fa78d397` were authored and pushed to
`origin/main` by a separate, concurrent process while `e02aec1e`…`5f05b3e2`
were being produced locally under the same work-item id (236). The collision
was only discovered locally at push time (`git push` reported the local
branch was "ahead 4, behind 2"), not during development — this is itself
part of the incident (see `07-concurrency-id-collision.md`).

### WI230 predecessor commits (for reconstructing when the 26 errors accumulated)

`2b114263` is WI230's closeout commit. Its own evidence run
(`evidence/runs/20260820-230-closeout.json`) and the FMR-numbered evidence
chain (`20260818-230-m0-agentbus-inventory.json` through
`20260820-230-m5-agentbus-retirement.json`) span 2026-08-18 through
2026-08-20 — a 3-day window. WI230's `work-item.json`
(`work/completed/230-*/work-item.json`) and its README document the M0–M5
milestone structure; the 26 RepoPact validation errors WI230 itself
accumulated are referenced in this session's WI236/237 investigation as a
starting condition, not independently re-derived here (see
`04-forgewire-case-timeline.md` for how this number was sourced and what
would be needed to verify it directly against a `repopact validate` run
pinned to a pre-WI236 commit).

**Post-hoc correction note (added after Phase 3's first pass):** Phase 3's
initial comparison used the checked-out feature branch
(`wi-032-ci-checkpoint-decision`) as "dev HEAD," which is *not* `origin/main`'s
current tip. Re-verification directly against `origin/main` found one
material discrepancy — the worktree-walk false-positive fix (PR #7,
`0096d70`) had already landed there on 2026-07-28, three weeks before this
incident — corrected throughout `01`, `03`, `05`, and `10`. All other
findings (id-allocation, README-heading-check absence, WI-032/decision 0031
status, live branch-protection and CI-run-status re-checks via `gh api`/
`gh run list`) were independently re-confirmed against `origin/main` directly
and hold as originally stated.

## RepoPact (development repository)

- Path: `C:\\local-path-redacted
- Branch: `wi-032-ci-checkpoint-decision` (not `main`/`master` — a feature
  branch checked out at capture time)
- HEAD: `8966ba060b5fe4a7dedd5aa65879db9dec6be2b0`
  ("governance: narrow the WI-032 CI blocker to an operator yes/no",
  2026-07-27 12:43:24 -0500)
- `VERSION` file: `3.0.0`
- Working tree: clean, up to date with `origin/wi-032-ci-checkpoint-decision`

This confirms current RepoPact development is materially ahead of the
`2.2.0` ForgeWire actually consumed — both in version number (2.2.0 → 3.0.0)
and in being mid-flight on its own work item (WI-032) about CI checkpoint
decisions at capture time, which is independently a subject worth reading
for Phase 3/5 given its apparent thematic overlap with this case study.

## RepoPact release snapshots

- Path: `C:\\local-path-redacted
- `2.2.0/`: contains only built distributions —
  `repopact-2.2.0-py3-none-any.whl` and `repopact-2.2.0.tar.gz`. No extracted
  source tree at this path; Phase 3 extracts these directly rather than
  relying on git history, since they are the literal artifact ForgeWire's
  `requirements-repopact.txt` pins and PyPI serves.
- `verify-2.2.0/`: a release-verification scaffold —
  `sandbox/` is a `repopact init`-produced repository (has its own
  `AGENTS.md`, `governance/`, `work/`, `evidence/`, etc.) and `venv/` is a
  Python virtualenv with `repopact==2.2.0` installed into it
  (`venv/Lib/site-packages`). This venv is the more direct way to inspect
  the exact 2.2.0 implementation as actually installed/run, parallel to how
  ForgeWire's own `.venv/Lib/site-packages` was inspected during the WI237
  session.

## RepoPact proving ground

- Path: `C:\\local-path-redacted
- Branch: `master`
- HEAD: `8f68bc4ba38aadfb85c11f51d0e21f89201cc274`
  ("governance: upgrade RepoPact to 2.2.0", 2026-07-18 14:50:06 -0500)
- Working tree: clean, up to date with `origin/master`

Notable: the proving ground's own HEAD commit is the RepoPact version
upgrade itself, dated 2026-07-18 — three days before this case study's
ForgeWire incident timeline even begins (WI230 created 2026-08-18 per its
evidence trail). This positions the proving ground as a possible source of
*designed* scenarios to compare against the *field* incident.

## Research corpus inventory (`C:\\local-path-redacted)

| File | Lines | Required by prompt | Notes |
| --- | --- | --- | --- |
| `paper.md` | 869 | yes | |
| `formal-model.md` | 429 | yes | |
| `protocol.md` | 143 | yes | |
| `benchmark-protocol.md` | 178 | yes | |
| `findings.md` | 211 | yes | |
| `threats-to-validity.md` | 106 | not named, directly relevant to Phase 9 | |
| `gap-audit-2026-07.md` | 285 | not named, dated within the incident window | |
| `figures.md` | 172 | supporting | |
| `paper-outline.md` | 92 | supporting | |
| `release-runbook.md` | 102 | supporting, relevant to Phase 3 (2.2.0 release process) | |
| `run-log.md` | 394 | supporting | |
| `README.md` | 39 | index | |

All twelve files were read in full for this case study, not selectively.
