# RepoPact runtime dependencies.
# Per decision 0003, records are validated against schemas/*.json with jsonschema.
jsonschema>=4.20
# Optional WI050 enforcement/signing support is installed with the
# ``repopact[enforcement]`` extra (or the development extra).
